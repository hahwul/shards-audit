# typed: strict
# frozen_string_literal: true

# This file is rendered by shards-audit's release pipeline (.github/workflows/publish-homebrew.yml).
# DO NOT EDIT by hand.
class ShardsAudit < Formula
  desc "Audit Crystal shard.yml/shard.lock dependencies for known vulnerabilities"
  homepage "https://github.com/hahwul/shards-audit"
  version "__VERSION__"
  license "MIT"

  on_macos do
    # macOS binaries are dynamically linked; openssl@3 is the only non-system
    # dependency (enforced by an otool guard in release-binary.yml).
    depends_on "openssl@3"

    on_arm do
      url "https://github.com/hahwul/shards-audit/releases/download/v__VERSION__/shards-audit-v__VERSION__-osx-arm64"
      sha256 "__SHA_OSX_ARM64__"
    end
    on_intel do
      url "https://github.com/hahwul/shards-audit/releases/download/v__VERSION__/shards-audit-v__VERSION__-osx-x86_64"
      sha256 "__SHA_OSX_X86_64__"
    end
  end

  on_linux do
    # Linux release binaries are statically linked (musl), so they are self-contained.
    on_arm do
      url "https://github.com/hahwul/shards-audit/releases/download/v__VERSION__/shards-audit-v__VERSION__-linux-arm64"
      sha256 "__SHA_LINUX_ARM64__"
    end
    on_intel do
      url "https://github.com/hahwul/shards-audit/releases/download/v__VERSION__/shards-audit-v__VERSION__-linux-x86_64"
      sha256 "__SHA_LINUX_X86_64__"
    end
  end

  def install
    bin.install Dir["shards-audit-v__VERSION__-*"].first => "shards-audit"
  end

  test do
    system "#{bin}/shards-audit", "--version"
  end
end
